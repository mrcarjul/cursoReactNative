import { StyleSheet } from "react-native";

export default StyleSheet.create({
  centerContents: {
    alignItems: "center",
    justifyContent: "center"
  },
  container: {
    flex: 1
  },
  title: {
    fontSize: 30,
    fontWeight: "bold",
    textAlign: "center"
  }
});
